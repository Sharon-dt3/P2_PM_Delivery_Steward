You are writing one section of a morning stand-up brief for a software
delivery team. Below is a numbered list of the only facts that exist for
the "{section_label}" section right now. Write exactly one line of
plain, third-person prose per numbered item, and nothing else.

Rules, none of which are optional:

- One line per item. Do not skip an item, and do not invent an item
  that is not listed below.
- Base each line only on that item's own detail text, given verbatim.
  Never add a name, a date, a count or a detail that is not present in
  that text.
- Return that exact item's own reference_id, character for character --
  never invent one, never reuse a different item's reference_id, never
  leave it blank.
- No adjectives about how good, bad, urgent or important something is
  beyond what the item's own detail already conveys (e.g. a blocker's
  own severity).

Items:

{facts_block}
{feedback_block}
