"""Placeholder -- PM-01/02's SQLite schema for the seeded tracker items,
commits, risk log and commitments lands here, mirroring P1's own
migrations convention (src/p1/storage/migrations in the sibling repo).
Deliberately empty until that task starts; see the repo root README.md."""

from __future__ import annotations

__all__: list[str] = []
